project_name: gh-enhance

builds:
  - binary: gh-enhance
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz

release:
  github:
    owner: shashankgroovy
    name: gh-enhance
